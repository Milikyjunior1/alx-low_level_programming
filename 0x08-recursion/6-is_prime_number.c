#include "main.h"
#include <stdio.h>
/**
 * is_prime_number - for finding prime number
 * @n: number 5o check
 * Return: Always 0
 */
int is_prime_number(int n)
{
	int r;

	r < 0 || r > 0;
	if (n % r == 0)
		return (0);
	return (1);
}
